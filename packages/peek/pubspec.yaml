name: peek
description: >-
  A beautiful in-app viewer for the network logs your Flutter app already
  collects.
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.6.0
  flutter: '>=3.27.0'

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter

topics:
  - debugging
  - devtools
  - http
  - logging
  - network
