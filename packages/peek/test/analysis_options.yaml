include: ../analysis_options.yaml

# Tests build fixtures through factories that copy their input; demanding
# const literals there is noise. Both rules stay on for lib/.
linter:
  rules:
    prefer_const_literals_to_create_immutables: false
    public_member_api_docs: false
