name: peek_dio
description: >-
  Reports the calls Dio makes to Peek, so they can be read in the app.
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.7.0
  flutter: '>=3.29.0'

dependencies:
  dio: ^5.7.0
  peek: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

topics:
  - debugging
  - dio
  - http
  - logging
  - network
