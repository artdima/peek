name: peek_dio
description: >-
  Reports the calls Dio makes to Peek, so they can be read in the app.
version: 1.4.0
resolution: workspace

repository: https://github.com/artdima/peek/tree/main/packages/peek_dio
issue_tracker: https://github.com/artdima/peek/issues
homepage: https://github.com/artdima/peek

environment:
  sdk: ^3.7.0
  flutter: '>=3.29.0'

dependencies:
  dio: ^5.10.0
  peek: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

topics:
  - debugging
  - dio
  - http
  - logging
  - network
