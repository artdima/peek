name: peek_talker
description: >-
  Reports the calls Talker logs to Peek, so they can be read in the app.
version: 1.4.0
resolution: workspace

repository: https://github.com/artdima/peek/tree/main/packages/peek_talker
issue_tracker: https://github.com/artdima/peek/issues
homepage: https://github.com/artdima/peek

environment:
  sdk: ^3.7.0
  flutter: '>=3.29.0'

dependencies:
  dio: ^5.10.0
  peek: ^1.0.0
  peek_dio: ^1.0.0
  talker: ^5.0.0
  talker_dio_logger: ^5.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter

topics:
  - debugging
  - http
  - logging
  - network
  - talker
